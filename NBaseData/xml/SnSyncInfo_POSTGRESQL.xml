<?xml version="1.0" encoding="utf-8" ?>

<databaseConfig>

	<sql name="GetRecords">
		SELECT

		HOST_NAME,
		MS_VESSEL_ID,
		MS_USER_ID,
		VESSEL_DATE,
		FROM_VESSEL_FLAG,
		SERVER_DATE,
		FROM_SERVER_FLAG,
		SYNC_STEP,
		MODULE_VERSION,
		MAX_DATA_NO_0,
		MAX_DATA_NO_1,
		SYNC_STATUS

		FROM SN_SYNC_INFO
	</sql>
	
	<sql name="ByHostName">
		WHERE HOST_NAME = :HOST_NAME
	</sql>
	
	<sql name="InsertRecord">
		INSERT INTO SN_SYNC_INFO(
		HOST_NAME,
		MS_VESSEL_ID,
		MS_USER_ID,
		VESSEL_DATE,
		FROM_VESSEL_FLAG,
		SERVER_DATE,
		FROM_SERVER_FLAG,
		SYNC_STEP,
		MODULE_VERSION,
		MAX_DATA_NO_0,
		MAX_DATA_NO_1,
		SYNC_STATUS
		)
		VALUES(
		:HOST_NAME,
		:MS_VESSEL_ID,
		:MS_USER_ID,
		:VESSEL_DATE,
		:FROM_VESSEL_FLAG,
		:SERVER_DATE,
		:FROM_SERVER_FLAG,
		:SYNC_STEP,
		:MODULE_VERSION,
		:MAX_DATA_NO_0,
		:MAX_DATA_NO_1,
		:SYNC_STATUS
		)
	</sql>

	<sql name="UpdateRecord">
		UPDATE SN_SYNC_INFO

		SET
		MS_VESSEL_ID = :MS_VESSEL_ID,
		MS_USER_ID = :MS_USER_ID,
		VESSEL_DATE = :VESSEL_DATE,
		FROM_VESSEL_FLAG = :FROM_VESSEL_FLAG,
		SERVER_DATE = :SERVER_DATE,
		FROM_SERVER_FLAG = :FROM_SERVER_FLAG,
		SYNC_STEP = :SYNC_STEP,
		MODULE_VERSION = :MODULE_VERSION,
		MAX_DATA_NO_0 = :MAX_DATA_NO_0,
		MAX_DATA_NO_1 = :MAX_DATA_NO_1,
		SYNC_STATUS = :SYNC_STATUS

		WHERE HOST_NAME = :HOST_NAME
	</sql>

</databaseConfig>
